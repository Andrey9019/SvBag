<div class="product-item">
  <a href="{{ product.url }}" style="color: {{ section.settings.text_color }};">
    <img src="{{ product.featured_image | img_url: 'medium' }}" alt="{{ product.title }}">
    <h3>{{ product.title }}</h3>
    <p>{{ product.price | money }}</p>
  </a>
</div>