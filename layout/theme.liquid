<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ shop.name }}</title>
  {{ content_for_header }}
  {{ 'base.css' | asset_url | stylesheet_tag }}

  {% style %}
  body {
background-color: {{ settings.homepage_background_color  }};
  }
  {% endstyle %}
</head>

<body>
{%  section 'header'  %} 
  <main >
    {{ content_for_layout }}
  </main>

</body>

</html>